# Database connection string
# For local development (SQLite):
DATABASE_URL="file:./dev.db"
# For production (Neon PostgreSQL):
# DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# JWT secret for signing tokens (CHANGE IN PRODUCTION — use a 64+ char random string)
JWT_SECRET="your-secret-key-here"

# Encryption secret for API keys (CHANGE IN PRODUCTION — use a separate 64+ char random string)
ENCRYPTION_SECRET="your-encryption-secret-here"

# Allowed frontend origin for CORS
CORS_ORIGIN="http://localhost:8080"

# Frontend URL for redirects
FRONTEND_URL="http://localhost:8080"

# Runtime environment
NODE_ENV="development"

# Server port (local dev only)
PORT=3001

# --- Voice / TTS (optional: use per-user API keys in Settings instead) ---
# ElevenLabs API key (fallback when user has no key)
# ELEVENLABS_API_KEY="..."
# Azure TTS (Cognitive Services Speech)
# AZURE_TTS_SUBSCRIPTION_KEY="..."
# AZURE_TTS_REGION="eastus"

# --- S3-compatible storage (required for voice.tts node output and voice assets) ---
# S3_BUCKET="mohtawa-voice"
# S3_REGION="us-east-1"
# S3_ACCESS_KEY_ID="..."
# S3_SECRET_ACCESS_KEY="..."
# S3_ENDPOINT="..." (optional; for R2/MinIO)
# S3_PUBLIC_BASE_URL="https://your-bucket.s3.region.amazonaws.com" (optional; for public URLs)
# S3_FORCE_PATH_STYLE="true" (optional; for MinIO)

# --- BullMQ (optional; when set, workflow execution runs in queue worker) ---
# REDIS_URL="redis://localhost:6379"
